<div class="home-header">
    <h1>Todos os Pensamentos</h1>

    <form action="/" method="get">
        <input type="text" name="search" placeholder="Está buscando por algo?">
        <button class="btn">Procurar</button>
    </form>

    <div class="order-container">
        <span>Ordernar por</span>

        <form action="/" method="get">
            <input type="hidden" name="search" value="{{this.search}}">
            <input type="hidden" name="order" value="new">
            <button>Novos</button>
        </form>
        <form action="/" method="get">
            <input type="hidden" name="search" value="{{this.search}}">
            <input type="hidden" name="order" value="old">
            <button>Antigos</button>
        </form>
        <a href="/">Limpar</a>
    </div>
</div>
<div class="thoughts-container">
    {{#if thoughts}}

    {{#each thoughts}}

    <figure>
        <blockquote>
            "{{this.title}}"
        </blockquote>
        <figcaption>
            por <span>{{this.User.name}}</span>
        </figcaption>
    </figure>

    {{/each}}

    {{else}}
    <p>Não existe nenhum pensamento cadastrado, <a href="/thought/add">clique aqui</a> para começar nessa jornada.</p>
    {{/if}}
</div>